
<!-- This page will only be viewable by the admin account -->
<h1>All Users</h1>

<div class = "user_list">
<ul id = "user_item">
  <% @users.each do |user| %>
    <li><%= link_to user.name, user_path(user) %></li>
<%end%>
</ul>

</div>

<h3>Login<h3>

<%= link_to "New User", new_user_path %>
